const frame=document.querySelector('#pov'),hint=document.querySelector('#hint'),state=document.querySelector('#state');let ws, reconnect, pressed=new Set();
const map={KeyW:'forward',KeyS:'back',KeyA:'left',KeyD:'right',ArrowUp:'forward',ArrowDown:'back',ArrowLeft:'left',ArrowRight:'right',Space:'jump',ShiftLeft:'sneak',ShiftRight:'sneak',ControlLeft:'sprint',ControlRight:'sprint'};
function send(value){if(ws?.readyState===WebSocket.OPEN)ws.send(JSON.stringify(value))}function release(){for(const key of pressed)send({type:'input',key,state:'up'});pressed.clear()}
function connect(){clearTimeout(reconnect);ws=new WebSocket(`${location.protocol==='https:'?'wss':'ws'}://${location.host}/controls`);ws.onopen=()=>state.textContent='Connected';ws.onclose=()=>{state.textContent='Reconnecting…';release();reconnect=setTimeout(connect,1200)}}connect();
document.addEventListener('keydown',e=>{const key=map[e.code];if(!key)return;if(['ArrowUp','ArrowDown','ArrowLeft','ArrowRight','Space'].includes(e.code))e.preventDefault();if(!pressed.has(key)){pressed.add(key);send({type:'input',key,state:'down'})}});document.addEventListener('keyup',e=>{const key=map[e.code];if(!key)return;e.preventDefault();if(pressed.delete(key))send({type:'input',key,state:'up'})});window.addEventListener('blur',release);window.addEventListener('beforeunload',release);
frame.addEventListener('load',()=>state.textContent='Connected');frame.addEventListener('click',()=>document.body.requestPointerLock());document.addEventListener('pointerlockchange',()=>document.body.classList.toggle('locked',document.pointerLockElement===document.body));document.addEventListener('mousemove',e=>{if(document.pointerLockElement===document.body)send({type:'look',dx:e.movementX,dy:e.movementY})});document.addEventListener('mousedown',e=>{if(document.pointerLockElement===document.body){e.preventDefault();send({type:'click',button:e.button===2?'right':'left'})}});document.addEventListener('contextmenu',e=>e.preventDefault());document.querySelector('#fullscreen').onclick=()=>document.documentElement.requestFullscreen?.();
